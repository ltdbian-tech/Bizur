# Copy to .env and fill in real values

# Server port
PORT=8787

# Firebase Admin SDK (paste the JSON inline or path to file)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Optional: restrict origins
ALLOWED_ORIGINS=*
